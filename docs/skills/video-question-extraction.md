# video-question-extraction — interior-craft delta

전기기사 하단 `N 번` 배지 파이프라인은 그대로 두고, 실내건축만 아래를 탄다.

- 나합격 클래스 화면: 좌상단 큰 숫자 `01`~`60` + 우측 Solution.
- 보기 `1)` · `42 | Solution` 은 번호 아님. `04 Solution` · 공백 두자리 `1 7` 은 번호.
- `num_range=(1,60)`, 정답 상한 60.
- 핵심요약은 자격 전체 100 (과목당 100 아님).
- 작업 경로: `/var/minis/workspace/interior-craft/`
